<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- 透明悬浮窗口主题（LauncherActivity 与后续速记页） -->
    <style name="Theme.Hexo.Transparent" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    <!-- LauncherActivity 禁止白屏启动预览 -->
    <style name="Theme.Hexo.Launcher" parent="Theme.Hexo.Transparent">
        <item name="android:windowDisablePreview">true</item>
    </style>
</resources>
